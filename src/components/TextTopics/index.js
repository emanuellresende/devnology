import { View, Text } from 'react-native'
import React from 'react'
import { Container } from './styles'

export default function TextTopics(props) {
    return (
        <Container>
            {props.name}

        </Container>
    )
}